{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core capabilities required by the TurboFiles window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
